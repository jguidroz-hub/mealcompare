{
  "name": "@mealcompare/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && cp manifest.json dist/ && cp src/popup/popup.html dist/popup/ && cp -r icons dist/"
  },
  "dependencies": {
    "@mealcompare/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0",
    "@types/chrome": "^0.0.287"
  }
}
