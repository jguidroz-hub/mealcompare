{
  "name": "@mealcompare/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && cp manifest.json dist/ && cp src/popup/popup.html dist/popup/ && mkdir -p dist/options && cp src/options/options.html dist/options/ && cp -r icons dist/"
  },
  "dependencies": {
    "@mealcompare/shared": "*",
    "@rollup/rollup-darwin-arm64": "^4.59.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0",
    "vite": "^6.2.0"
  }
}
