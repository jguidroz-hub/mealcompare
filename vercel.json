{
  "buildCommand": "npm -w packages/shared run build && npm -w packages/engine run build && npm -w packages/web run build",
  "outputDirectory": "packages/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
